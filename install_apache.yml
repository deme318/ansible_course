---
- hosts: all
  become: true
  tasks:

  - name: Update repository index
    ansible.builtin.apt:
      update_cache: yes

  - name: Install apache2
    ansible.builtin.apt:
      name: apache2
      state: latest

  - name: Install net-tools
    ansible.builtin.apt:
      name: net-tools
